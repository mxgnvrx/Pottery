# Баг 01. Переполнение слота: бронь заполненного занятия проходит

## 1. Симптом бага
Бронь слота с `remaining_places = 0` возвращала статус 201 и увеличивала `booked_count` выше лимита.

## 2. Ссылка на бизнес-требование
* **Требование:** R-004

## 3. Причина возникновения и Реализация исправления
* **Причина:** Проверка в `create_booking` использовала строгое неравенство `remaining < 0` вместо `<=`.
* **Фикс:** Условие изменено на `if remaining <= 0: return jsonify(...), 409`.

## 4. Отправленные промпты
> Прогони TC-10 через Flask test client. Почему бронь заполненного слота возвращает 201 вместо 409? Найди причину в create_booking и исправь.

## 5. Результат ручной проверки и Commit
* **Статус:** Исправлено. Поведение системы верифицировано вручную, баг полностью устранен и не воспроизводится.
* **Commit:** `fix: запрет брони заполненных и отменённых слотов (R-004, R-008) + UI`