# Этап 1. Аналитика: требования к MVP

> Отчёт о генерации требований с ИИ. Источник — `docs/brief-pottery.md`
> (письмо заказчика Марины + уточнения по скоупу).

## 1. Цель этапа

Из сырого, местами противоречивого письма заказчика выделить **минимальный
жизнеспособный продукт (MVP)**: сформулировать User Stories и ключевые сценарии
использования для **клиентского** приложения записи в мастерскую «Глина».

## 2. Границы MVP (что делаем / что нет)

Согласно уточнениям по скоупу в брифе, вся инфраструктура (бэкенд-источник,
интерфейсы мастера и админа) уже существует. **Предмет работы — только роль
Клиент (R-028):** мобильное клиентское приложение и API для него.

| В скоупе MVP | Вне скоупа MVP (отложено / чужая зона) |
|---|---|
| Просмотр расписания (7 дней) | Онлайн-оплата (пока наличные/перевод) |
| Карточка занятия с деталями | Оценка мастера после занятия |
| Бронирование места + прокат | Push-уведомления (упомянуты, но не в MVP) |
| Обработка отказа «нет мест» (409) | Программа лояльности |
| Запрет записи на отменённые слоты | Интерфейсы мастера / владельца (уже есть) |

## 3. Роли

* **Клиент** — единственная роль текущей поставки (R-028). Пользуется с телефона,
  ищет свободные слоты и записывается.
* *(Мастер, Владелец/Админ — существуют в инфраструктуре, вне скоупа.)*

## 4. User Stories

### Эпик A. Каталог расписания
* **US-A1.** Как клиент, я хочу видеть список ближайших занятий на **7 дней вперёд**
  (R-027), чтобы быстро выбрать подходящее время.
* **US-A2.** Как клиент, я хочу видеть по каждому слоту программу, дату/время,
  мастера и число свободных мест, чтобы принять решение без лишних кликов.
* **US-A3.** Как клиент, если занятий нет, я хочу видеть понятное сообщение
  **empty state** («Пока нет доступных занятий»), а не пустой экран.
* **US-A4.** Как клиент, я хочу отличать заполненные и отменённые занятия от
  свободных по визуальному признаку (бейдж/цвет).

### Эпик B. Карточка занятия
* **US-B1.** Как клиент, я хочу открыть карточку слота и увидеть описание
  программы, длительность, вместимость, рейтинг мастера и адрес мастерской.
* **US-B2.** Как клиент, я хочу видеть, доступен ли **прокат инструментов и фартука**
  и сколько он стоит (без выбора размера — специфика брифа).
* **US-B3.** Как клиент, если занятие отменено мастерской, я хочу видеть причину
  отмены и понимать, что запись недоступна (R-008).

### Эпик C. Бронирование
* **US-C1.** Как клиент, я хочу записаться на свободный слот, указав имя и телефон,
  чтобы гарантированно занять место.
* **US-C2.** Как клиент, я хочу опционально отметить, что беру инструменты и фартук
  напрокат.
* **US-C3.** Как клиент, я хочу получить экран подтверждения записи с деталями,
  чтобы убедиться, что всё прошло успешно.
* **US-C4.** Как клиент, если пока я оформлял бронь места закончились, я хочу
  увидеть понятную ошибку, а не «молчаливый» провал (R-004, ответ 409).

## 5. Ключевые сценарии использования (Use Cases)

### UC-1. Просмотр расписания и запись (happy path)
1. Клиент открывает приложение → видит список занятий на 7 дней.
2. Выбирает занятие «Гончарный круг, сб 18:00, свободно 4 из 6».
3. Открывает карточку, читает детали, отмечает «прокат фартука».
4. Вводит имя и телефон, нажимает «Подтвердить запись».
5. Система атомарно резервирует место → экран успеха с деталями.

### UC-2. Запись на заполненный слот (защита от двойных броней)
1. Клиент открывает карточку слота, где остаётся 1 место.
2. Пока он заполняет форму, это место занимает другой клиент.
3. Клиент нажимает «Подтвердить» → бэкенд возвращает **409 Conflict**.
4. Приложение показывает «Мест не осталось», не создавая ложную бронь (R-004).

### UC-3. Отменённое мастерской занятие
1. У мастерской форс-мажор (сломалась печь) → слот получает статус
   `CANCELLED_BY_STUDIO` в инфраструктуре.
2. Клиент видит слот в списке с бейджем «Отменено мастерской».
3. В карточке отображается причина; форма записи скрыта. Повторная запись
   **запрещена** (R-008).

### UC-4. Пустое расписание
1. На ближайшие 7 дней занятий нет.
2. Клиент видит empty state вместо пустого экрана (US-A3).

## 6. Бизнес-правила (сведены из брифа)

| ID | Правило |
|----|---------|
| R-027 | Дефолтный горизонт расписания — строго **7 дней** от текущей даты. |
| R-cap | Круг для новичков — до **6** человек; лепка — до **10** человек. |
| R-rent | Прокат инструментов и фартука — **без размеров**. |
| R-004 | **Атомарная** проверка мест; нельзя забронировать при `remaining <= 0` → **409**. |
| R-008 | Запрет записи на слоты со статусом `CANCELLED_BY_STUDIO`. |

## 7. Критерии приёмки (Definition of Done)

* Расписание по умолчанию показывает слоты за 7 дней; пустой период → empty state.
* Карточка отражает все поля контракта, включая прокат и адрес.
* Бронь свободного слота → 201 и экран успеха.
* Бронь заполненного слота → 409, без изменения данных.
* Бронь отменённого слота → отклонена, форма недоступна.

---

## Приложение. Использованные промпты

**Промпт 1 (постановка контекста):**
> Роль: Senior Full-Stack разработчик и системный аналитик. Проект — MVP
> клиентского приложения записи в гончарную мастерскую «Глина». Вот бриф
> заказчика (письмо + уточнения по скоупу): <вставлен текст `brief-pottery.md`>.
> Проанализируй бриф и определи границы MVP: что входит в клиентскую поставку,
> а что вне скоупа. Выдай результат чистым Markdown.

**Промпт 2 (генерация требований):**
> На основе брифа сгенерируй требования к MVP: роли, User Stories, сгруппированные
> по эпикам (Каталог, Карточка, Бронирование), и ключевые сценарии использования
> (happy path + краевые: заполненный слот, отменённый слот, пустое расписание).
> Учитывай бизнес-правила: горизонт 7 дней (R-027), вместимость круг/лепка 6/10,
> прокат без размеров, атомарная проверка мест с ответом 409 (R-004), запрет
> записи на CANCELLED_BY_STUDIO (R-008). Добавь критерии приёмки.

**Промпт 3 (сведение бизнес-правил):**
> Из письма и уточнений выпиши таблицу бизнес-правил с ID (R-027, R-004, R-008
> и т.д.) — по одному правилу на строку, кратко и однозначно.
